# Существующие системы

## Сначала понять систему

**Ситуация.** Задача затрагивает готовую систему, сервис, компонент или workflow.

**Действия.**

1. Найти владельца поведения, точку входа, состояние, контракт и потребителей.
2. Проследить путь данных до эффекта. Проверить документацию, тесты и runtime-факты.
3. Если механизм уже есть, использовать его в месте задачи. Если контракт не покрывает требование, менять систему у владельца и проверить потребителей.

**Результат.** До кода понятны путь выполнения, уровень изменения и проверка. Локальный костыль не создаёт параллельное правило.

## Решение держать у владельца инварианта

**Ситуация.** Правка добавляет guard, filter, fallback, shortcut или новое ветвление.

**Действия.**

1. Назвать инвариант и слой с полным контекстом для решения.
2. Проверить, не дублирует ли правка ответственность другого слоя.
3. Оставить одно место решения; нижним слоям передавать готовый результат или явный контракт.
4. Проверить все call sites и граничные сценарии владельца.

**Результат.** Один владелец правила, нет расходящихся guards и symptom patch.

Для сценария просмотра отдельно проверить разрешения чтения, личной информации и изменения прогресса. Общее состояние «сервис недоступен» может скрывать корректный запрет записи, ошибочно применённый к чтению. Предлагаемое UI-действие проследить до конечного эффекта: вход в существующий аккаунт может заменять локальный прогресс. При разделении результатов проверить потребителей: ошибка личного запроса должна отличаться от отсутствующей записи.

## Готовность первого UI-кадра

**Ситуация.** Новый UI Toolkit экран меняет размер центральной композиции сразу после открытия.

**Действия.** Сопоставить момент добавления дерева в panel, асинхронные ожидания, подключение `GeometryChangedEvent` и первый расчёт scale/frame. Измерить геометрию до и после этого расчёта. Проверить повторный вход: новое дерево теряет inline-стили прежнего. Готовность первого показа закреплять в lifecycle экрана: отдельно подготовить скрытое дерево, применить геометрию, синхронно связать элементы и показать. Загрузку каталога, иконок и сетевых данных вынести в отдельный этап. Подготовка нового дерева должна сохранять состояние ещё видимого контроллера, включая повторный вход в тот же экран.

**Результат.** Поздний расчёт геометрии отделён от загрузки спрайтов; итоговый неподвижный скриншот дополнен проверкой начального состояния.

Источник: [расследование скачка UI](../Planning/ui_screen_layout_jump_2026-09-05.md).

Источники: [конвенции кода](../rules/code_conventions.md), [подготовка C#-реализации](../../.github/prompts/csharp-implementation-preparation.prompt.md), [self-review](../../.github/prompts/manual-stages/self-review-after-implementation.prompt.md).

## Процентная ширина внутри масштабируемого макета

Когда центральный блок должен занимать долю экрана, проверить ширину его design-контейнера после `Fit`. При ограничении масштаба высотой исходный design оставляет боковые поля. Для адаптивной вёрстки задать `design.width = viewport.width / scale`, сохранить равномерный scale и назначить центральному блоку процентную ширину. Вложенные панели и списки перевести на доли родителя. Проверить world bounds на 16:9 и широком landscape.

## Проверять покрытие редизайна UI

**Ситуация.** Нужно найти экраны, оставшиеся после массового обновления интерфейса.

**Действия.** Сопоставить реестр экранов, места показа модалок, UI из runtime-кода и активные компоненты сцен. Для каждого проверить UXML/USS, переопределения общего host, используемые ассеты и историю представления. Готовность макета, импорт PNG и подключение к живому UI фиксировать отдельно. Переименованное переиспользование уточнять по ссылкам и при необходимости SHA256.

**Результат.** Список содержит действующие визуальные хвосты. Общие ассеты, обновления одной логики и оставшиеся шаблоны классифицированы по реальному использованию.

При доработке кнопок проверить и UXML, и runtime-создание: назначить общий art-класс и класс геометрии, убрать прежние inline-стили. Отступ варианта задавать селектором сильнее базового `margin: 0`. Для общей правой колонки дать контейнеру значения `flex-grow: 1`, кнопкам — одинаковую ширину и фиксированный отступ от текста.

Единый вид подписей проверять по размеру, гарнитуре и регистру. Регистр задавать после локализации при первом показе, смене языка и динамических состояниях. У `LocalizedButton` учитывать отложенную установку текста: она тоже должна применять выбранный регистр.

## Подписки EditorWindow на runtime singleton

Если runtime-сервис сбрасывает `Instance` в `SubsystemRegistration`, редакторское окно переподключает подписку при смене Play Mode и смене экземпляра. Отписывать прежний экземпляр перед привязкой нового и при закрытии окна. Это требуется и при отключённом Domain Reload; окно читает актуальное состояние общего сервиса.

## Проверять сохранение через чтение после перезапуска

При изменении границы сохранения проверить native serializer → storage → native reader, затем перезапустить runtime. `JsonUtility` может записать null вложенного класса как объект с пустыми полями. Нормализовать такой sentinel у владельца чтения, сохранив проверку частично заполненных данных. Успешная запись и правильный JSON сами по себе не подтверждают успешное восстановление профиля.
