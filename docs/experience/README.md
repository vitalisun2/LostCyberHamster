# База опыта агентов

Проверенные инструкции по инструментам, реализации, ошибкам, скорости и качеству.

## Карта документов

- [README.md](README.md) — применение, запись и рост базы.
- [Существующие системы](existing_systems.md) — изучить контракт и выбрать правильный уровень изменения.
- [Диагностика ошибок](error_diagnosis.md) — доказать причину, проверить наблюдаемость, собрать точные факты.
- [Итерации и проверки](iterative_delivery.md) — двигаться малыми шагами и проверять достаточным способом.
- [Общее рабочее дерево](shared_worktree.md) — сохранить чужие изменения и свой scope.
- [Использование инструментов](tool_usage.md) — выбирать узкие проектные команды, диагностику и документацию.
- [Работа без сети](offline_validation.md) — различать холодный запуск, потерю сети, SDK-имитацию и проверку устройства.
- [Импорт спрайтов](#импорт-спрайтов) — сверять ссылки с фактически загруженными ресурсами Unity.

## После первой неудачной попытки

Триггер: изменение сделано, но задача, баг, фича или ожидаемое поведение не получены.

До следующей правки:

1. По карте прочитать релевантную тему или ближайший кейс. Без карты прочитать основной `README.md` Experience.
2. Если опыт не подходит или не решает проблему, создать нужное число доступных субагентов и дать им конкретные неповторяющиеся направления исследования.
3. Сопоставить их факты, устранить расхождения, выбрать обоснованный подход. Без поддержки субагентов выполнить те же исследования самому и указать причину.
4. Только затем вернуться к реализации и проверить целевое поведение.

Новая попытка требует нового факта, гипотезы или подхода. Повтор локальных правок без новой основы запрещён.

## Запись и рост

- Писать только подтверждённый, повторно применимый вывод: ситуация, действия, проверяемый результат.
- Перед записью искать смысловые дубли в `docs/experience/`; уточнение добавлять в существующий кейс.
- Одиночную запись новой темы держать в `README.md` под `## Новые записи` и отражать в карте.
- При двух связанных записях самостоятельной темы создать `lowercase_snake_case.md`, перенести туда детали, а в карте оставить одну ссылку с описанием.
- Каждый вывод хранить в одном месте. Историю задачи оставлять в её отчёте.

## Новые записи

### Импорт спрайтов

Если Sprite исчезает при существующих PNG и согласованных GUID/fileID, проверить `AssetImporter.GetAtPath`, `AssetDatabase.LoadAllAssetsAtPath` и ошибки импорта в `Editor.log`. Сравнить метаданные рабочего и проблемного варианта. Пустой элемент `physicsShape` в `.png.meta` может сорвать разбор Unity и импорт всей текстуры. При восстановлении сохранить GUID и sprite fileID; результат проверить по фактическим Sprite, стартовому кадру prefab, кадрам clips и physics manifest.

При расширении кадра сохранить PPU и положение pivot относительно рисунка. `ISpritePhysicsOutlineDataProvider` хранит точки относительно центра прямоугольника: к ним добавить `(oldSize - newSize) / 2 + (paddingLeft, paddingBottom)`. Пиксельный pivot сдвинуть на `(paddingLeft, paddingBottom)`. После импорта сравнить реальные точки `Sprite.GetPhysicsShape`, GUID/fileID и пройти все ключи clips через `SkinVisualHost` в preview scene. Тайминг просмотра брать из Unity clips.

### Запас плашки под локализацию

Если runtime-текст расположен на художественной плашке, её safe area рассчитывается под максимальное число строк поддерживаемых языков. Для основного и вторичного текста задаются отдельные непересекающиеся зоны с внутренними отступами.
