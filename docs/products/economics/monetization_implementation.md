# Монетизация — состояние работы

Дата: 2026-09-10. Задача `01a08b59-5fcd-7673-817d-5ad8258db289`, local `integration/unity-live`. [План 16 задач](monetization_plan.md), [точечное исследование](research/21_monetization_launch_policy.md), [brief](monetization_brief.md).

## Готовая подготовка

- Предложен мягкий кандидат interstitial: 3 победы, 15 минут активной игры, 1/UTC-день со второго посещения; live выключен. Исходные 3/10/2 остаются кандидатом дальнейшего теста. Мягкую частоту пользователь выбрал; универсальная безопасность не заявляется.
- Рекомендована видимость Starter B — после фактического открытия платного скина за очко. A с ранним замком тоже показан. Уровень 2 сам по себе условием не является.
- Определены IAP 5 pipeline, SKU-кандидаты, локальные цены, идемпотентное сохранение до ConfirmPurchase и внешняя серверная проверка. Коммерческая настройка/платежи ещё не проверены.
- Реальные исходные экраны сняты через unity-ui-capture; HTML-этап исключён по последнему указанию пользователя. Общие skills не менялись. Task-local wrapper выполняет штатный capture и PNG export.

## Доказательства capture

Папка: `.temp/monetization-ui/capture/` от корня репозитория. `run.json`: success true, Play stopped true, profileRestored true, lockReleased true. 3/3 PNG, 2532×1170. Editor 6000.2.6f2, чистая Bootstrap, ревизия `7b1590067085447f1dd4771da26533a7d14d68db`.

| Исходник | Подготовка |
|---|---|
| `shop--original.png` | Штатный ShopScreenController/каталог, изолированный профиль 500 монет/20 кристаллов |
| `win--original.png` | Актуальный Win блока 4; реальные controller/NextGoal, демонстрационные Submitted/1200/+20 XP; статичный игровой фон |
| `lose--original.png` | Реальный Lose; текущее состояние сервиса; статичный игровой фон |

Профиль восстановлен сравнением JSON адаптера. Прямой вызов модалок не подтверждает переходы полного игрового flow. Видео, платежи и отправка демонстрационного результата не запускались.

В исходном магазине обнаружено расхождение рисунка с каталогом: на рисунке «+10 CRYSTALS» за 500 монет, реальная операция выдаёт 1 кристалл. В концептах показан действующий обмен 500/1; изменение рисунка входит в будущую согласованную UI-интеграцию.

## Концепты и выбор

Встроенный ImageGen, `referenced_image_paths` с актуальными PNG после просмотра. Изображения показаны прямо в задаче. Промпты и исходные пути — `.temp/monetization-ui/concepts/prompts.json`; рабочие PNG в той же папке. Это AI-концепты, не снимки реализованного UI.

| Участок | Вариант A | Вариант B | Рекомендация |
|---|---|---|---|
| Lose M3 | `loseA.png`: вся фраза в одну строку с кнопками | `loseB.png`: «Продолжить за» над рядом кнопок | B: сохраняет ширину окна |
| Win M2 | `winA.png`: бонус под звёздами, оранжевое действие | `winB.png`: бонус после результатов, синее действие | B: обычный Next Level остаётся главным |
| Win M4 | `pauseA.png`: заметная плашка перед кнопками | `pauseB.png`: строка под кнопками | A: лучше заметно предстоящее прерывание |
| Shop M1/M5/M6/M7/M8 | `shopA.png`: компактная сетка, Starter виден с замком | `shopB.png`: Starter появляется после открытия, широкая верхняя карточка до этого | B: предложение связано с доступной целью |
| Состояния Shop | `shopAState.png`: Starter открыт, No Ads куплен, cooldown | `shopBState.png`: Starter открыт, покупка обрабатывается, cooldown | Следуют выбранному каркасу |

Во всех вариантах сохранён визуальный язык исходной игры. Цены USD — пример концепта; runtime получает локальную цену магазина. В финальной вёрстке длинный заголовок No Ads допускает перенос, inactive cooldown не вызывает видео; растровые неточности вроде подчёркивания числа в Win B не задают семантику действия.

**Пользователь выбрал все B:** Shop, Win (`exec-2ee5499d...`), Lose (`exec-d158b886...`), пауза (`exec-a6154ded...`). Shop B дополнен снижением насыщенности фонов на ~50%; ресурсы/кнопки остаются яркими. `shopBMuted.png` показан повторно, его итоговый вид подтверждается. Мягкая частота 3 победы / 15 минут / максимум 1 interstitial за UTC-день выбрана отдельно; 0 показов допустимо. Новый UI/финальные спрайты не интегрированы. Следующий шаг после выбора — подготовка отдельных ассетов с динамическими текстами и последовательная интеграция модулей по плану.

## Владение и проверки

- Блок 4 передал Win/Win-селекторы USS/Menu/RU-EN после scoped commit `7b159006`. Его код чист на момент передачи.
- Координатор подтвердил отсутствие активных пересечений logic scope PlayerData/Ads/ShopManager/RunCoinBudget/UiLose/CheckpointReason с блоками 1/2/3/5.
- Menu/RU-EN временно переданы активному retention UI-интегратору `01a08aec-a032-7361-9734-fd39e41afa58`; перед UI-правками согласовать обратно. Его Home/Quests/Skills/Hero scope сохраняется.
- Retention передал окно Unity для M1 logic/compile/Play. Работа выполнена под собственным lock; после commit окно возвращается retention.
- Подготовка UI проверена capture 3/3, просмотром каждого варианта и сверкой с brief. M1 C# проверен отдельно ниже.
- Внешние IAP/Ads кабинеты, облачная верификация/выдача и измеренный revenue-source остаются зависимостями research 21. Реальные списания/публикация билда не выполнялись.

Собственный Git scope: этот отчёт, план, research21; RewardedAdService/Policy, PlayerData/MonetizationState, PlayerDataValidator, ShopManager, Unity meta двух новых файлов и generated Assembly-CSharp.csproj. PNG/capture-подготовка остаются вне Assets. Чужие README/brief/research19/20/AGENTS/retention UI и EditorLogs в собственный commit не включаются.

Ретроспектива: готовые research/fixture повторно использованы; первый capture успешен; изменения UI остановлены на пользовательском gate. Лишний build вызван непроверенным импортом новых файлов; применён существующий приём tool_usage.md: проверить stopped/import/Compile Include перед compile. Новая запись опыта не требуется.


## M1-L — выполнено

Cooldown 10 минут хранится в PlayerData.Monetization.LastShopRewardUtcTicks. Старый профиль без отметки доступен сразу. CanRequestShop/ShopManager ограничивают магазин; Revive использует прежний CanRequest. Награда остаётся каталоговой — 50 монет. Награда, request ID и время сохраняются одной транзакцией. Повторный ID не выдаёт валюту и не сдвигает время. Tick сообщает изменения доступности/остатка раз в секунду.

Проверки:

- После штатного импорта оба новых Compile Include присутствуют. Regeneration и dotnet build Assembly-CSharp.csproj --no-restore: 0 errors / 42 warnings; `.temp/monetization-ui/m1-build.log`. Первый build использовал stale csproj: Editor был в Play. Остановка/AssetDatabase.Refresh/regeneration устранили причину без C#-правки.
- Минимальный Play: Home виден, профиль загружен, testingBackup=false, Console errors 0; Play остановлен. `m1-observation.json`, `m1-console.json` в той же папке.
- Read-only eval политики/JSON: старый профиль 0 секунд, новая выдача 600, через 599 секунд остаток 1, через 600 остаток 0, JSON round-trip точный, откат часов на минуту даёт 660 секунд. Профиль этим eval не менялся.
- Review execution path: повторные terminal callbacks отсекает TerminalReceived; skip/cancel до completion не вызывают Settle. AppliedRewardedRequestIds отсекает повторную выдачу/сдвиг cooldown; ошибка записи откатывает envelope, completion journal повторяет обычный Settle. Реальный SDK-досмотр и сбой дисковой записи здесь не воспроизводились; это review, не runtime-доказательство этих сценариев.

M1-U ожидает согласованной интеграции Shop: текущая кнопка получает доступность от менеджера; новый текст таймера ещё не добавлен. M2–M8 остаются задачами плана. Перед M2 отделить Win placement от текущего non-revive магазинного Settle. Локальные часы не обеспечивают доверенную защиту между устройствами.
