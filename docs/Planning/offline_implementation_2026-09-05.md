# Реализация работы без интернета — 5 сентября 2026

Код основных систем переведён на локальное сохранение с фоновым восстановлением сетевых функций. C# компилируется. Финальная приёмка на Android требует установленного APK и устройства: ADB пока возвращает пустой список.

Основа: [исходный аудит](offline_audit_2026-09-05.md) и утверждённый пользователем план. Аудит описывает состояние до реализации; результаты ниже относятся к новым исходникам.

## Что изменено

| Подсистема | Реализация |
|---|---|
| Запуск | `LicenseManager` больше не создаёт сетевую блокировку. Обязательная загрузка остаётся локальной; UGS, аккаунт и Analytics запускаются в фоне. Ошибка чтения локальных данных показывается в Bootstrap. |
| Сохранение | Зашифрованный `LocalSaveEnvelope`: формат, профиль, подтверждённый владелец, игровые ревизии, облачная база, журналы. Рабочее состояние забега отделено от последнего записанного снимка. |
| Миграция | Старый `PlayerData` переносится в контейнер. Валидный primary сохраняется в backup. Повреждённый primary восстанавливается из backup. Более новая схема сохраняется для обновления игры. |
| Checkpoints | `GameDataManager.ExecuteTransaction` фиксирует изменения и журналы вместе; `PlayerProgressCommitter` уведомляет после записи. Технические ACK сохраняют уже записанный gameplay, сохраняя игровую дату и ревизию. |
| Ошибка записи | Экономическая операция откатывается. Ошибка обычного checkpoint сохраняет заработанное рабочее состояние для повтора. Пользователь получает локальное сообщение об ошибке. |
| Экономика | Покупки, обмен, развитие, награды заданий, XP и рекламные награды фиксируются до успешного уведомления. У обмена есть `requestId` и последние 64 receipts; повтор подтверждённой операции идемпотентен. |
| Аккаунт | Ошибка первого входа и expiry запускают восстановление. Известный владелец сохраняется. Public SDK profiles и durable mapping сохраняют credentials исходного аккаунта при переключении и перезапуске. |
| OAuth | Браузерный вход ограничен 120 секундами активного приложения. Отмена освобождает ожидание. Встроенный Player Accounts SDK проверяет generation и OAuth state; Windows listener закрывается. |
| Повторы | Общий планировщик: 5, 15, 30, 60 секунд, затем до 60 секунд с jitter. Одна операция каждого сервиса в полёте. Foreground и ручной повтор объединяют немедленные попытки. Reachability используется как подсказка. |
| Облако | Отправляется сохранённый snapshot. Durable attempt содержит ID, ревизию, hash и ожидаемый write lock. Потерянный ACK сверяется с облаком; подтверждение старой ревизии сохраняет новые изменения ожидающими. |
| Конфликт | Обе версии и отсрочка сохраняются. Выбор целого снимка: устройство, облако, позже. Выбор повторно проверяет данные; позднее чтение после «Позже» отменено. Замена профиля выполняется в безопасном меню. |
| Реклама | Один `RewardedAdRequest` связывает requestId, профиль, награду и runId. Подготовка ограничена 15 секундами. SDK COMPLETED принимается после потери связи; повтор callback поглощается receipt. |
| Watchdog рекламы | 10 секунд без старта и 120 секунд активного ожидания дают диагностическое состояние. Окончательное решение остаётся у SDK. Обычная навигация доступна после возврата из рекламного окна. |
| Восстановление награды | Подтверждённая награда магазина восстанавливается из журнала независимо от экрана. Одно намерение после перезапуска награду не выдаёт. Revive относится только к исходному активному забегу. |
| Недельные результаты | `WeeklyLeaderboardCoordinator` хранит FIFO в `WeeklyLeaderboardJournal`. Run содержит профиль, владельца, среду, таблицу, исходный VersionId, score, runId и состояние подтверждения. |
| Первый офлайн-запуск | Результат без известной серверной недели записывается как `LocalOnly`. После входа он сохраняет этот статус. |
| Недельные XP | Подтверждение сохраняется; 50 XP и `AppliedWeeklyRewardRunIds` записываются вместе. Потерянный ответ проверяется по собственной metadata. Неопределённый результат завершается без XP. При конфликте выдача и отправка приостановлены. |
| Задания | Местная полночь, SetId, InstanceId, использованные даты. Повтор даты сохраняет текущий набор. Защищённые задания сохраняют экземпляр; доступная общая награда прежнего набора переносится отдельным правом. |
| Рейтинг | Кэш разделён по владельцу, среде, таблице и серверной неделе; показаны время и прошлый период. Пустое состояние содержит повтор. Поздние ответы ушедшего экрана игнорируются. |
| Аналитика | Сохраняется штатная очередь SDK. Инициализация повторяется в фоне; подписки идемпотентны. Используется актуальный Unity Consent API с сохранением существующего выбора. |
| Диагностика | Устойчивая очередь: до 5 payload по 1 МиБ, 7 дней. Повторы причины объединяются, успешная отправка удаляет запись. Старый endpoint сохраняет свой срок хранения и пропускает новые отправки к текущему адресу. |

## Основные точки кода

- [Локальный контейнер и транзакции](../../LostCyberHamster/Assets/Scripts/GameManagement/Persistence/GameDataManager.cs), [LocalSaveEnvelope](../../LostCyberHamster/Assets/Scripts/GameManagement/Persistence/LocalSaveEnvelope.cs).
- [Сетевые повторы](../../LostCyberHamster/Assets/Scripts/Online/OnlineServicesCoordinator.cs), [AccountService](../../LostCyberHamster/Assets/Scripts/Account/AccountService.cs).
- [CloudSyncService](../../LostCyberHamster/Assets/Scripts/CloudSave/CloudSyncService.cs), [ConflictService](../../LostCyberHamster/Assets/Scripts/CloudSave/ConflictService.cs).
- [RewardedAdService](../../LostCyberHamster/Assets/Scripts/Ads/RewardedAdService.cs), [ShopManager](../../LostCyberHamster/Assets/Scripts/SharedCore/Meta/Shop/ShopManager.cs).
- [Недельная очередь и кэш](../../LostCyberHamster/Assets/Scripts/Leaderboard/WeeklyLeaderboardCoordinator.cs), [дневные наборы](../../LostCyberHamster/Assets/Scripts/SharedCore/Meta/Quests/Daily/DailyQuestService.cs).
- [Устойчивая диагностика](../../LostCyberHamster/Assets/Scripts/Diagnostics/DeviceLogUploadRunner.cs).

Локальные журналы, SDK profile mapping, повторы и отсрочка конфликта остаются на устройстве. Облачный снимок содержит игровой `PlayerData` и применённые игровые receipts.

## Проверки

| Проверка | Результат |
|---|---|
| Независимый review сохранений, ACK, owner и конфликтов | Найденные ошибки рабочего rollback и позднего выбора исправлены. |
| Независимый review аккаунта и рекламы | Исправлены потеря исходных credentials, зависший OAuth, recovery первого owner bind и блокировка навигации при settlement. |
| Независимый review недельных результатов | Исправлены потеря результата при первой ошибке диска, проверка недели перед terminal compare и сохранение `LocalOnly`. |
| Review заданий | Подтверждён порядок balance + XP + claimed, перенос общей награды и восстановление runtime после rollback. |
| Unity import/recompile | Прошёл для импортированного среза. |
| Generated C# gate | `regenerate_project_files`, `dotnet build Assembly-CSharp.csproj --no-restore`: 0 ошибок. `Unity.Services.Authentication.PlayerAccounts.csproj --no-restore`: 0 ошибок. Финальный срез фиксируется вместе с APK. |
| Android preflight | Прошёл: Editor 6000.2.6f2, Android SDK/NDK/JDK, подпись и проверка Editor references доступны. |
| 12 таблиц в репозитории | 12/12: keepBest, descending, понедельник 00:00 UTC. |
| Работающее окружение UGS | Live-конфигурации пока не прочитаны; требуется сверка development и production перед выпуском. |
| Физический Android | ADB не видит устройства. Сценарии ниже ожидают прогона конкретного APK. |

Review и компиляция подтверждают исходники. Они отделены от проверки Android callbacks, физического отключения сети и устойчивости хранилища при завершении процесса.

## Android-приёмка

Использовать отдельный тестовый профиль. Зафиксировать build ID, модель телефона, Android, исходный баланс и время каждого сценария. Диагностику читать штатным способом.

1. Чистая установка; до первого старта включить авиарежим. Открыть меню, обучение, доступный уровень, настройки, персонажа. Зафиксировать отсутствие сетевой блокировки.
2. Завершить уровень, получить награду, обменять ресурсы. Принудительно завершить процесс; повторный запуск должен восстановить баланс, награды и прогресс.
3. Подключить Wi-Fi без доступа в интернет. Повторить запуск и забег; меню остаётся доступным, сетевые функции показывают ожидание.
4. Во время забега отключить сеть, закончить уровень, перейти в меню. Затем вернуть сеть: тот же аккаунт и очереди восстанавливаются без перезапуска.
5. Проверить известный аккаунт после истечения сессии. Отдельно закрыть браузер входа через Back; дождаться завершения ожидания и повторить вход.
6. Изменить обе версии профиля на двух устройствах. Проверить обе карточки, ресурсы, прогресс, уровень героя и дату игрового сохранения. Выбрать «Позже», перезапустить, продолжить игру и открыть выбор через настройки.
7. Оборвать ответ после cloud write; сделать новое локальное изменение. После связи подтверждается отправленный снимок, новое изменение остаётся ожидающим.
8. Реклама: double tap, отказ SDK, потеря сети после Show, возврат из фона, поздний и повторный callback. Проверить одну награду, доступность остальных действий и revive исходного runId.
9. Уйти из магазина во время рекламы. Подтверждённая награда должна пережить закрытый экран и перезапуск после записи receipt.
10. Сохранить несколько улучшений недельного результата офлайн. После связи проверить FIFO и 50 XP за каждое доказанное улучшение. При смене недели старый результат завершается в исходном периоде.
11. На первом полностью офлайн-запуске получить результат. После запуска онлайн он остаётся `LocalOnly` и сохраняется между запусками.
12. Проверить полночь, несколько пропущенных дней, часовую зону и возврат к использованной дате. Проверить защищённые задания и отдельное право на общую награду.
13. На тестовых данных проверить старый формат, повреждённый primary, валидный backup и более новую схему. Рабочую копию сохранить до сценария.
14. Проверить длительную автономную работу: очереди, ограничение логов, повторы и отсутствие сетевых блокировок.

## Границы надёжности

- Недельная клиентская очередь допускает потерю бонусных XP, если принятие операции уже нельзя доказать после lost ACK или смены недели.
- Рекламный SDK не восстанавливает доказательство просмотра после гибели процесса до записи completion. Одно сохранённое намерение награду не даёт.
- Primary/backup работает в пределах гарантий текущего PlayerPrefs-хранилища. Постоянная ошибка устройства требует успешной повторной записи.
- Локальное время защищает от повторного использования даты. Ранее не посещённая дата, в том числе прошлая, считается новым периодом по принятому правилу.
- **Дополнительный риск SDK:** при одновременной самой первой cloud-записи два устройства могут прочитать отсутствующий ключ. `SaveItem(..., null)` игнорирует write conflict для ключа, успевшего появиться. Известные версии защищены write lock. Строгая защита первого создания требует серверного арбитража или отдельного протокола хранения; дополнительное чтение лишь сужает окно гонки.

## Совместная работа

Cloud-карточки, их UXML/USS и локализация интегрируются владельцем параллельной UI-задачи. Offline QA APK использует согласованный стабильный срез Cloud + HUD. Остальные этапы UI имеют собственную приёмку. Чужие незавершённые изменения сохраняются отдельно от offline scope.
